#version 330 core

in vec4 outCols;
varying float isCircular;


void main(){

    if(isCircular){
        float R = 1.0;
        float R2 = 0.5f;
        float dist = sqrt(dot())
    }
    gl_FragColor = outCols;
}